---
id: formiz
title: <Formiz> component
---

import { WIP, NotImplemented } from '../../src/messages'

## Props

#### `onSubmit(values)`

> Triggered when the form is **submitted**.

#### `onValidSubmit(values)`

> Triggered when the form is **valid** and **submitted**.

#### `onInvalidSubmit(values)`

> Triggered when the form is **invalid** and **submitted**.

#### `onChange(values)`

> Triggered every time the form change.<br />
> ⚠️ This is triggered when each field is mounted.<br />
> ℹ️ Instead you can get `values` of the form with the [useForm()](/docs/core/use-form) hook.

#### `onValid()`

> Triggered when the form is **valid**.<br />
> ℹ️ Instead you can get `isValid` value with the [useForm()](/docs/core/use-form) hook.

#### `onInvalid()`

> Triggered when the form is **invalid**.<br />
> ℹ️ Instead you can get `isValid` value with the [useForm()](/docs/core/use-form) hook.

#### `autoForm`

> Set to `true` to auto add a `<form>` element with auto onSubmit.<br />
> ℹ️ Instead you can get the `submit()` method with the [useForm()](/docs/core/use-form) hook and use it with your own `<form>` element.

#### `connect`

> Allow you to connect your form with the [useForm()](/docs/core/use-form) hook.<br />
> See [useForm() documentation](/docs/core/use-form) for more details

---

## Handle API errors

<WIP />

```jsx
const myForm = useForm()

<Formiz
  connect={myForm}
  onValidSubmit={(values) => {
    myForm.invalidateFields({
      fieldName: 'Error message',
    })
  }}
/>
  // Put your fields here
</Formiz>
```