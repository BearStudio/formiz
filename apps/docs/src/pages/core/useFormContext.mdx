# useFormContext

This hook works like [`useForm()`](/core/useForm), with the difference that it doesn't create store. `useFormContext()` should be use only as a form children.

## Import

```tsx
import { useFormContext } from "@formiz/core";
```

## Refer to [`useForm()`](/core/useForm)
