export const Type = ({ type }: { type: string }) => {
  return (
    <p
      style={{
        fontSize: "1rem",
        fontWeight: "200",
        marginTop: "1rem",
      }}
    >
      {type}
    </p>
  );
};
